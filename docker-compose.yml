services:
  telegram-bot:
    build: .
    container_name: telegram_bot
    restart: unless-stopped
    expose:
      - "8080"
    env_file:
      - .env
    networks:
      - proxy_network
      - app-net

networks:
  proxy_network:
    external: true
  app-net:
    external: true